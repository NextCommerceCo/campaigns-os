node_modules/
node_modules
.DS_Store
.campaign-runtime/
!contracts/fixtures/sidecar-bundle/production-shaped/.campaign-runtime/
!contracts/fixtures/sidecar-bundle/production-shaped/.campaign-runtime/*.json
coverage/
dist/
npm-debug.log*

# Local QA verdict output (`campaigns-os qa run` default --output-dir).
# Redundant with the QA portal for published runs; only unique for offline/CI runs.
# Ignored so QA runs are never accidentally committed into PRs.
qa-output/
